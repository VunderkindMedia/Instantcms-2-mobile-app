import { Router, CommonAction, NavigationState, DefaultRouterOptions } from '../../types';
export declare type MockActions = CommonAction | {
    type: 'NOOP' | 'UPDATE';
};
export declare const MockRouterKey: {
    current: number;
};
export default function MockRouter(options: DefaultRouterOptions): Router<NavigationState, MockActions>;
